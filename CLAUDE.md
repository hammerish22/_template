# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Environment

- Python 3.14 virtual environment at `.venv/` (already created, excluded from git)
- Activate: `.venv\Scripts\Activate.ps1` (PowerShell) or `.venv\Scripts\activate` (bash)
- Install packages: `pip install <package>` (with venv active)

## Notes

This repository is a template/starting point with no application code yet. As the project grows, update this file with:
- Build, lint, and test commands
- Architecture overview
- Key conventions
